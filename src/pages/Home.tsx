import React from "react";
import styled from "styled-components";

const Home = () => {
  return <Container>보드겠또 홈</Container>;
};

export default Home;

const Container = styled.section`
  padding: 30px;
`;
